---
function generageCategory(category) {
  // 将historical-data转换成Historical Data
  if (!category) return ''
  return category
    .split('-')
    .map((word) => word.charAt(0).toUpperCase() + word.slice(1))
    .join(' ')
}
const { articles } = Astro.props
---

<div class="my-24">
  <hgroup class="my-12">
    <h2 class="font-serif font-bold text-neutral-900 text-5xl">Archive</h2>
  </hgroup>
  <ul class="space-y-8 pl-0">
    {articles.map((article) => (
      <li class="list-none">
        <a
          class="no-underline"
          href={`/archive/detail/${article.slug}`}>
          <h3 class="font-serif font-bold text-3xl text-neutral-900 mb-3 hover:underline">{article.title}</h3>
          <small class="font-sans text-base text-neutral-900 opacity-50">{generageCategory(article.category)}</small>
        </a>
      </li>
    ))}
  </ul>
</div>
